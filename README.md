# cloud-pool-managers.github.io

Landing page de **Cloud Pool Manager** (Hugo, layout autonome — pas de thème externe).

- En ligne : **https://cloud-pool-managers.github.io**
- Local : `hugo server`

Le push sur `main` lance le workflow `deploy-pages` (build Hugo → GitHub Pages).
